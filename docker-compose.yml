services:
  app:
    image: aqua-chroma
    container_name: aqua-chroma-app
    restart: unless-stopped
    ports:
      - "8010:8000"
    volumes:
      - aquq-data:/app/data
    env_file:
      - .env

volumes:
  aquq-data: